/*
 * Spotify Messages
 *
 * This contains all the text for the Spotify container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Spotify';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Spotify',
  },
});
